import 'package:flutter/material.dart';

class AppDisplayLogo extends StatelessWidget {
  const AppDisplayLogo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
